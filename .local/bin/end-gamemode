#!/usr/bin/env sh

# notify-send "Ending gamemode.."

systemctl --user start wlsunset.service

if [ "$XDG_CURRENT_DESKTOP" = "Hyprland" ]; then
    if [ "$(hyprctl getoption animations:enabled | awk 'NR==1{print $2}')" = 0 ]; then
        hyprctl reload
    fi
fi
